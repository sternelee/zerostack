[package]
name = "zerostack-extension-api"
version.workspace = true
edition.workspace = true
description = "API crate for zerostack plugins — WIT definitions and generated bindings"
license.workspace = true
homepage.workspace = true
repository.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
wit-bindgen = "0.58"
